<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lightnovelbackend.dao.ChapterMapper">
    <select id="getChapters" resultType="Chapter">
        select * from chapter where book_id=#{book_id} and volume_id=#{volume_id}
    </select>
</mapper>